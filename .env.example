# Google Sheets webhook (Apps Script Web App URL). Empty = mock mode (localStorage only).
VITE_SHEETS_URL=
# Optional shared secret — must match the TOKEN Script Property. Empty = open endpoint.
VITE_SHEETS_TOKEN=
